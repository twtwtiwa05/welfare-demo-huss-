# 복지망 잔여 사각지대 발굴·모니터링 AI 에이전트 — 웹 데모

![React](https://img.shields.io/badge/React-18.3-61DAFB?logo=react&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-5.6-3178C6?logo=typescript&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-5.4-646CFF?logo=vite&logoColor=white)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-3.4-06B6D4?logo=tailwindcss&logoColor=white)
![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)
[![Live Demo](https://img.shields.io/badge/Live_Demo-온라인_바로보기-000000?logo=vercel&logoColor=white)](https://welfare-demo-huss.vercel.app/)

### 🔗 라이브 데모 (Quick Start)

> **별도 설치 없이 바로 확인하세요 → https://welfare-demo-huss.vercel.app/**
>
> 최신 브라우저에서 위 주소로 접속하면 5단계 분석 파이프라인·활용도 비교·모니터링 대시보드를 그대로 사용할 수 있습니다. 로컬 실행 방법은 [시작하기](#시작하기) 절을 참고하세요.

---

투명한 위험 점수 모형과 설명 가능한 근거 생성을 결합한 **복지 사각지대 발굴 보조 시스템**의 개념 증명(PoC) 웹 데모입니다. 보건복지부 ‘복지 사각지대 발굴관리시스템(행복e음)’ 같은 기존 광역 발굴 체계를 **대체하지 않고**, 그 발굴 결과의 **해석 가능성·활용도**를 높이는 보조 레이어(assistive layer)를 지향합니다.

> ⚠️ **합성 데이터 기반 데모입니다.** 실제 발굴 성능을 증명하지 않으며, 분석 파이프라인·근거 생성·대시보드 UX의 작동을 검증하는 것이 목적입니다. 자세한 범위는 [한계와 책임](#한계와-책임) 절을 참고하세요.

---

## 목차

- [개요](#개요)
- [핵심 설계 원칙](#핵심-설계-원칙)
- [주요 기능](#주요-기능)
- [기술 스택](#기술-스택)
- [아키텍처](#아키텍처)
- [위험 점수 모형](#위험-점수-모형)
- [프로젝트 구조](#프로젝트-구조)
- [시작하기](#시작하기)
- [빌드 & 배포](#빌드--배포)
- [한계와 책임](#한계와-책임)
- [제작자](#제작자)
- [라이선스](#라이선스)

---

## 개요

기존 발굴 시스템은 다수의 위기정보를 융합해 위험가구를 선제적으로 발굴하지만, 산출된 명단이 **왜 위험한지**를 충분히 설명하지 못해 현장 담당자의 신뢰·활용으로 이어지지 않는 한계가 있습니다. 본 데모는 다음 세 지점에 집중합니다.

1. **재해석** — 단일 임계값(OR) 판정이 아니라 여러 신호의 가중 조합으로 위험을 평가하여, 개별 신호는 기준 미달이지만 동시에 위험 신호가 누적된 가구를 후보로 포착합니다.
2. **집단 특화** — 전국 단위 범용 모형 대신 대상 집단(독거노인 / 중장년 1인가구)별로 가중치를 분기하여, 집단 특수성을 반영합니다.
3. **활용도** — 점수와 신호를 근거로 한 자연어 설명·권고·우선순위·시계열 추적을 제공하여, 명단을 ‘행동’으로 전환합니다.

데모는 **위험 점수 산출(투명 모형)**, **근거 생성(LLM 역할 시뮬레이션)**, **모니터링 대시보드**의 세 레이어로 구성됩니다.

---

## 핵심 설계 원칙

본 데모는 네 가지 설계 원칙을 코드 수준에서 강제합니다. 이는 시스템의 신뢰성·설명가능성·책임 소재를 보장하기 위한 제약입니다.

| 원칙 | 내용 | 구현 |
|---|---|---|
| **투명한 점수 산출** | 위험 점수는 가중합 공식이 계산합니다. 신경망/LLM이 점수를 만들지 않으며, 입력 변경 시 즉시 재계산되는 결정론적 함수입니다. | `src/lib/scoring.ts` |
| **역할이 한정된 AI** | 근거 생성기는 *이미 산출된* 점수와 신호를 입력받아 자연어 설명·권고만 생성합니다. 위험 여부나 점수를 판단하지 않습니다. | `src/lib/reason.ts` |
| **계산과 시뮬레이션의 명시적 구분** | 실측 계산 영역과 시뮬레이션(미리 정의된 값) 영역을 UI에 라벨링합니다. | `SimBadge`, `ProofBoundary` |
| **사람 중심 의사결정** | 최종 개입(전화·방문) 결정은 담당자가 합니다. AI는 발굴·근거·우선순위까지만 관여하며, ‘자동 처리’ 동작은 존재하지 않습니다. | `CaseDetail` (조치 ‘기록’만) |

---

## 주요 기능

### 1. 5단계 분석 파이프라인
데이터가 각 단계에서 실제로 변형되는 과정을 시각화합니다.

| 단계 | 기능 | 비고 |
|---|---|---|
| ① 원시 신호 | 가구별 위험 신호(전력·진료·우편·통신·고립) 표시 | 데이터 표시 |
| ② 정합·중복제거 | 기존 시스템이 이미 포착한 가구를 제외하고 ‘잔여’ 후보만 분석 | 보완 레이어 |
| ③ 투명 점수 산출 | 가중합 공식으로 점수 계산. 슬라이더 실시간 재계산 + 집단 모형 전환 | 결정론적 계산 |
| ④ 근거 생성 | 점수·신호 기반 자연어 근거·권고·반대근거·출처 생성 | LLM 역할 시뮬 |
| ⑤ 우선순위 정렬 | 임계값 방식과 조합 방식 대조, 거짓음성(재현율) 지표 | 두 방식 비교 |

### 2. 활용도 비교 (Before / After)
동일한 발굴 명단을 **점수만 제시한 형태**와 **근거·권고·추세 배지가 결합된 형태**로 대조하여, 정보 구조가 현장 의사결정에 미치는 차이를 보여줍니다.

### 3. 모니터링 대시보드
행정동별 위험 밀집도 히트맵, 주간 급상승 가구, 위험도 시계열, 케이스 상세(근거·권고·조치 기록)를 제공합니다.

---

## 기술 스택

| 구분 | 기술 | 버전 | 선택 이유 |
|---|---|---|---|
| 언어 | TypeScript | 5.6 | 정적 타입 안정성 |
| UI 프레임워크 | React | 18.3 | 컴포넌트 기반 선언적 UI |
| 빌드 도구 | Vite | 5.4 | 빠른 개발 서버·번들링 |
| 스타일 | Tailwind CSS | 3.4 | 유틸리티 기반 일관된 디자인 |
| 차트 | Recharts | 2.15 | 위험도 시계열 시각화 |
| 아이콘 | lucide-react | 0.46 | 경량 SVG 아이콘 |
| 상태 관리 | React Hooks | — | `useState` 기반 (외부 상태 라이브러리 미사용) |

> 의존성 버전은 호환성 확보를 위해 고정되어 있습니다. 백엔드·데이터베이스·인증 계층은 사용하지 않으며, 전체가 **클라이언트 전용 정적 애플리케이션**으로 동작합니다.

---

## 아키텍처

### 설계 개요
- **클라이언트 전용 SPA** — 서버·API 키가 필요 없어 임의의 정적 호스팅에 배포 가능합니다.
- **도메인 로직과 프레젠테이션 분리** — 점수·우선순위·근거 등 핵심 로직은 `src/lib`의 순수 함수로 구현되어 UI(`src/components`)와 독립적으로 테스트·재사용됩니다.
- **결정론적 파이프라인** — 동일 입력에 대해 항상 동일 출력을 반환합니다. 점수 모형과 근거 생성 모두 부수효과가 없습니다.

### 데이터 흐름
```
scripts/generate-data.mjs        (시드 고정 난수 → 합성 가구 생성)
        │
        ▼
src/data/households.json         (정적 데이터셋, 70가구)
        │
        ▼
src/lib/*  ── scoring · profiles · priority · reason · dedup · history
   (순수 도메인 로직: 정규화 → 가중합 → 구간 판정 → 근거·우선순위)
        │
        ▼
src/components/*                 (React 프레젠테이션: 파이프라인 · 대시보드)
        │
        ▼
       UI
```

### 근거 생성 (LLM 역할)
데모에서는 LLM 호출을 **결정론적 템플릿**(`src/lib/reason.ts`)으로 시뮬레이션합니다. 점수·신호·케이스 유형으로부터 자연어 근거, 권고 2건, 반대 근거·불확실성, 근거 출처(신호 칩)를 구조화된 형태로 생성합니다. 실서비스 적용 시 이 레이어를 LLM API로 대체하되, 다음 제약을 유지하도록 설계되어 있습니다.

- 점수는 클라이언트의 투명 모형이 계산해 입력으로 전달하고, LLM은 점수를 재계산하지 않습니다(설명·서술만).
- API 키는 서버 측에서만 사용하고 브라우저에 노출하지 않습니다.
- 호출 실패 시 시뮬레이션 결과로 폴백합니다.

---

## 위험 점수 모형

각 신호를 0~1로 정규화한 뒤 집단별 가중치를 곱해 합산합니다(0~100 스케일).

```
정규화:
  power     = min(전력 사용 급감률 / 100, 1)
  medical   = min(진료 단절 일수 / 365, 1)
  mail      = min(우편 미수령 주 / 4, 1)
  telecom   = min(통신 연체 개월 / 3, 1)
  isolation = (복지관 6개월 이용 0회) ? 1 : 0

점수 = round( Σ (정규화_i × 가중치_i) )

위험 구간: 65점 이상 = 고위험 / 45점 이상 = 주의 / 그 외 = 관찰
```

**집단별 가중치 프로파일** (합 100) — 동일 가구라도 적용 모형에 따라 점수가 달라집니다.

| 신호 | 독거노인 (elderly) | 중장년 1인가구 (middleaged) |
|---|---|---|
| 전력 급감 (power) | 25 | 30 |
| 진료 단절 (medical) | 30 | 15 |
| 우편 미수령 (mail) | 25 | 15 |
| 통신 연체 (telecom) | 10 | 20 |
| 사회적 고립 (isolation) | 10 | 20 |

**발굴 방식 비교**
- 임계값 방식(기존): 단일 신호가 임계 초과 시 발굴 (`power≥80 || medical≥365 || mail≥4 || telecom≥3`)
- 조합 방식(본 데모): 가중합 점수 ≥ 50 이면 발굴

---

## 프로젝트 구조

```
welfare-demo/
├── scripts/
│   └── generate-data.mjs        # 합성 데이터 생성 (시드 고정, 재현 가능)
├── src/
│   ├── data/households.json     # 합성 가구 데이터셋 (생성물)
│   ├── lib/                     # 도메인 로직 (순수 함수)
│   │   ├── types.ts             #   도메인 타입
│   │   ├── scoring.ts           #   투명 점수 공식 · 신호 메타 · 위험 구간
│   │   ├── profiles.ts          #   집단별 가중치 프로파일
│   │   ├── dedup.ts             #   기존 시스템 기포착 분리
│   │   ├── priority.ts          #   임계값 vs 조합 + 거짓음성 집계
│   │   ├── reason.ts            #   근거 생성기 (LLM 역할 시뮬)
│   │   ├── history.ts           #   시계열 추세 헬퍼
│   │   ├── caseLabels.ts        #   케이스 유형 라벨
│   │   └── data.ts              #   데이터 로더
│   ├── components/              # 프레젠테이션 (React)
│   │   ├── PipelineView.tsx     #   5단계 파이프라인 컨테이너
│   │   ├── Step1~5*.tsx         #   각 단계 화면
│   │   ├── BeforeAfterView.tsx  #   활용도 비교
│   │   ├── ProofBoundary.tsx    #   검증 범위 고지 패널
│   │   ├── Dashboard.tsx        #   대시보드 컨테이너
│   │   ├── Heatmap / CandidateList / CaseDetail / RisingTop / RiskTimeline
│   │   └── SimBadge / RiskBadge / StepProgressBar
│   ├── styles/index.css
│   ├── App.tsx                  # 레이아웃 · 탭 라우팅
│   └── main.tsx                 # 진입점
├── index.html
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.*.json
└── package.json
```

---

## 시작하기

### 요구 사항
- Node.js 18 이상 (개발 환경 기준 22.x)

### 설치 및 실행
```bash
npm install        # 의존성 설치
npm run dev        # 개발 서버 실행 → http://localhost:5173
```

### 스크립트
| 명령 | 설명 |
|---|---|
| `npm run dev` | 개발 서버 (HMR) |
| `npm run build` | 타입 체크(`tsc -b`) 후 프로덕션 빌드 → `dist/` |
| `npm run preview` | 빌드 결과 로컬 미리보기 |
| `npm run gen:data` | 합성 데이터 재생성 (시드 고정) |

---

## 빌드 & 배포

```bash
npm run build      # dist/ 정적 산출물 생성
```

산출물 `dist/`는 정적 파일이므로 Vercel·Netlify·Cloudflare Pages·GitHub Pages 등 임의의 정적 호스팅에 배포할 수 있습니다. 별도의 환경 변수나 서버 런타임이 필요하지 않습니다.

**Vercel 배포 예시**: 저장소를 import하면 Vite 프리셋이 자동 인식되며, 추가 설정 없이 빌드·배포됩니다. 이후 `main` 브랜치 푸시 시 자동 재배포됩니다.

---

## 한계와 책임

본 데모는 다음을 **명확히 구분**합니다.

**증명하는 것**
- 분석 파이프라인의 동작
- 위험 점수의 투명성·재현성
- 근거 기반 정보 구조가 의사결정 보조에 기여하는 방식(UX)

**증명하지 않는 것**
- 실제 발굴 정확도 및 예측 성능 (정답 레이블을 합성으로 설계했으므로 성능 평가는 순환적입니다)
- 정량적 발굴량 비교 ("몇 배" 등의 수치)

**전제 및 윤리적 고려**
- 합성 데이터셋이며, 기존 시스템 기포착 여부(`haengbokFlagged`)는 시뮬레이션 플래그입니다.
- 실제 적용은 개인정보보호 법령, 지자체 위임, 실데이터 연동을 전제로 합니다.
- 모든 데이터는 가명처리·집계 분석을 전제로 하며, 재식별은 법적 통제 하에서만 수행되어야 합니다.

---

## 제작자

**김태우 (Taewoo Kim)**
가천대학교 스마트시티학과 학부생
📧 twdaniel@gachon.ac.kr

---

## 라이선스

본 프로젝트는 [MIT License](./LICENSE) 하에 배포됩니다.
